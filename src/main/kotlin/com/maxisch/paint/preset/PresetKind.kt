package com.maxisch.paint.preset

/** Which folder a preset name lives in; used when a rename has to fix up the bindings. */
enum class PresetKind { BLOCKS, ROOMS, BOSSES, TYPES, PALETTES, RULESETS }
